break;  do;     insttanceof;    typeof; case;
else;   new;    var;    catch;  finally;
feturn;     void;   continue;   for;    switch;
while;  default;    if;     throw;  delete;
in;     try;    function;   this;   with;
debuggre;   false;  true;   null;
